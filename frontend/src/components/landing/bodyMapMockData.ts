/** Example paper counts per merged body region (swap for API / filtered data). */
export const DEFAULT_PAPER_COUNTS: Record<string, number> = {
  head: 96,
  torso: 210,
  arms: 43,
  legs: 120,
  hands: 223,
  feet: 76,
};
